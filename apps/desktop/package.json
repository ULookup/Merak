{
  "name": "merak-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "check": "node scripts/check-prereqs.mjs",
    "build": "node scripts/check-prereqs.mjs && node scripts/prepare-runtime.mjs && tauri build",
    "prepare-runtime": "node scripts/prepare-runtime.mjs",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
